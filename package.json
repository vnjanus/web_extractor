{
  "name": "web-text-extractor",
  "version": "1.0.0",
  "description": "Extract text from webpages using Playwright with human fingerprinting",
  "main": "dist/server.js",
  "scripts": {
    "dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "@types/marked": "^5.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "marked": "^16.0.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.18",
    "@types/node": "^20.8.0",
    "nodemon": "^3.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.2.0"
  }
}
